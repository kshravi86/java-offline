import SwiftUI

struct ContentView: View {
    var body: some View {
        // The app should just show an empty blank page for now
        Color.white.ignoresSafeArea()
    }
}

#Preview {
    ContentView()
}
